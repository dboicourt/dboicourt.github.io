//sentance variable is the input from the working array converted into a string, should already be defined
function capsLock() {
    if (capsCheck === true) {
        
        return (displayString.toUpperCase())
    } else {
        return displayString
    }
}